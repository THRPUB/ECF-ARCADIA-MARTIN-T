<form method="post" action="{{ path('app_administrateur_delete', {'administrateur_id': administrateur.administrateur_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ administrateur.administrateur_id) }}">
    <button class="btn">Delete</button>
</form>
