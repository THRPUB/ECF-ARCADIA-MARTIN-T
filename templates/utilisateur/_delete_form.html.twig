<form method="post" action="{{ path('app_utilisateur_delete', {'utilisateur_id': utilisateur.utilisateur_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ utilisateur.utilisateur_id) }}">
    <button class="btn">Delete</button>
</form>
