{% extends 'base.html.twig' %}

{% block title %}Pagecontact{% endblock %}

{% block body %}
    <h1>Pagecontact</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pagecontact.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ pagecontact.Nom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ pagecontact.Email }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ pagecontact.Message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pagecontact_index') }}">back to list</a>

    <a href="{{ path('app_pagecontact_edit', {'id': pagecontact.id}) }}">edit</a>

    {{ include('pagecontact/_delete_form.html.twig') }}
{% endblock %}
