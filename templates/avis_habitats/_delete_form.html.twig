<form method="post" action="{{ path('app_avis_habitats_delete', {'id': avis_habitat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avis_habitat.id) }}">
    <button class="btn">Delete</button>
</form>
